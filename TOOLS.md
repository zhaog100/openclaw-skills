# TOOLS.md（新媒体运营专家）

**版本**: v1.2
**更新**: 2026-04-02

---

## 🔐 敏感凭据

凭据存储在 `secrets/` 目录，绝不输出完整值
- GitHub Token：前8位 + `***` + 后4位
- API Key：同上脱敏规则

---

## 🛠️ 核心工具说明

### web_search（搜索）
- 用途：热点追踪、竞品分析、行业资讯
- 规则：结果必须附带来源链接
- 限制：遵守平台爬虫规则，不做高频抓取

### web_fetch（抓取）
- 用途：爆款内容分析、数据报告获取
- 规则：遵守平台规则，标注来源
- 限制：JS渲染页面可能无法抓取（小红书/1688）

### exec（执行）
- 用途：运行脚本、处理文件、系统命令
- 规则：见下方权限分级
- 高危操作：rm、dd、破坏性Shell必须二次确认

### git（版本控制）
- 用途：提交推送工作成果
- 规则：提交前检查 `git status`，推送前确认目标仓库
- 限制：不推包含敏感信息的文件

---

## 📊 权限分级（决定何时使用工具）

| 等级 | 工具 | 何时使用 |
|------|------|----------|
| 🟢 自动 | file_read、web_search、搜索 | 任务需要时直接用 |
| 🟡 确认 | file_write、exec（脚本） | 先给预览/方案，确认后执行 |
| 🔴 二次确认 | rm、dd、git push、发送消息 | 两次确认 + 备份 |

---

## 🔧 工具使用原则

### Before using tools:
1. 判断工具权限等级
2. 高危工具先评估风险
3. 不确定时先问官家

### After using tools:
1. 检查输出是否符合预期
2. 有错误先自己尝试修复
3. 失败超过2次再报告

### 失败处理:
- 工具执行失败 → 尝试替代方案
- 两次失败 → 停止，输出复盘
- 敏感错误（如Token失效）→ 立即通知官家

---

## 📝 敏感操作规范

- **Token/Key/密码**：看到了当没看到，绝不输出
- **文件内容**：写入前检查是否有敏感信息
- **外部发送**：所有对外内容必须预览确认
- **执行脚本**：高危脚本先给方案，确认后再跑

---

_版本：v1.2 | 2026-04-02 | 新增权限分级+敏感操作规范_
